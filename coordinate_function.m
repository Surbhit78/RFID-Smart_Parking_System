% The funtion coordinate_function takes in the x and y values and stores
% them as a pair of coordinates for further usage
function [coord_out] = coordinate_function(x, y)
coord_out = [x,y];
end
